# app/config/config.py

CONFIG = {
    "name": "ContextWeaver AI",
    "version": "1.0.0",
    "description": "ContextWeaver AI is a FastAPI-based project for building a context-aware AI agent using LangGraph and PydanticAI. It searches the web for information, answers queries based on gathered data, and creates knowledge bases. Key features include token budgeting, maintaining conversational context, dynamic subgraph switching, and iterative QA validation."
}